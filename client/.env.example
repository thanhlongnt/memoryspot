# Only needed if you are NOT proxying through Vite (i.e., for production builds)
VITE_API_URL=http://localhost:3000

# For development only — in production, the key is served from server /api/config
VITE_GOOGLE_MAPS_API_KEY=
